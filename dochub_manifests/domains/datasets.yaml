datasets:
  docs.all_systems:
    source: >
      (
          components.$spread().{
          "Система": $[0].*.title,
          "Сущность": $[0].*.entity,
          "Статус": $[0].*.status,
          "Описание": $[0].*.description
        }[Сущность = "system"]
      )